# Postgres (used by postgres service + backend)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=processo_seletivo
DATABASE_HOST=postgres
DATABASE_PORT=5432
DATABASE_USER=postgres
DATABASE_PASS=postgres
DATABASE_NAME=processo_seletivo

# Backend
PORT=5005
JWT_SECRET_TO_SIGN=your-jwt-secret-here
SECRET_OR_KEY=your-jwt-secret-here
# Comma-separated CORS origins (admin :3000; candidate PWA :3001)
WEB_ORIGIN=http://localhost:3000,http://localhost:3001

# Frontend (server-side inside Docker uses backend hostname)
API_URL=http://backend:5005
NEXT_PUBLIC_API_URL=http://localhost:5005

# pgAdmin (compose profile: tools)
PGADMIN_DEFAULT_EMAIL=admin@admin.com
PGADMIN_DEFAULT_PASSWORD=admin
